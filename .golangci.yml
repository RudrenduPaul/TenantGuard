version: "2"
run:
  timeout: 5m
linters:
  default: standard
  enable:
    - gocritic
    - unconvert
    - unparam
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - unparam
