# TA10 clean fixture — the agent explicitly declares both per-agent overrides.
agents:
  - name: research-agent-a
    tenant: tenant-a
    overrides:
      workspace_restriction: true
      sandbox_config:
        cpu_limit: "1"
