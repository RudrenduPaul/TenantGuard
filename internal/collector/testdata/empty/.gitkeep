This directory intentionally contains no *.yml/*.yaml files — used by
TestEmptyTargetErrors to confirm collector.Collect errors loudly on a target
with nothing to scan, rather than silently returning a clean empty result.
